import { resolve } from 'path';
import processFilesInDirectory from 'utils/process_files_in_directory';
export { default as sqlite } from './sqlite';
import 'model/user_modal';

// processFilesInDirectory(resolve('./src', 'model'));
